from django.contrib import admin

# Les modèles métier (facturation) ne sont pas enregistrés dans l'admin.
# La gestion des factures, clients et paiements se fait exclusivement
# via l'interface applicative.
